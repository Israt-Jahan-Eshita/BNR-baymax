services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: baymax
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - baymax_data:/var/lib/postgresql/data
volumes:
  baymax_data:
